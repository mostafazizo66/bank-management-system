#ifndef COLORS_H_
#define COLORS_H_

#include <SFML/Graphics.hpp>

static const sf::Color basicRed(255, 0, 0);
static const sf::Color activeRed(255, 100, 100);
static const sf::Color basicGreen(0, 200, 0);
static const sf::Color activeGreen(60, 255, 60);
static const sf::Color basicBlue(60, 60, 250);
static const sf::Color activeBlue(150, 150, 255);
static const sf::Color darkBlue(0, 0, 160);
static const sf::Color pressedButton(0, 35, 112);

#endif